# Invalid, non-authoritative, and closed evidence summary

## INVALID / NON-AUTHORITATIVE
- Recovery-only missing-c15 Horner run (`max ~0.0007061958`) is INVALID.
- Historical full47 semantic value around `24.879` came from mismatched provenance and is INVALID.
- Extracted-suffix propagation metrics are NON-AUTHORITATIVE because cutting the graph changed ORT downstream optimization context (~`6.58e-5` reference drift).
- Synthetic Holdout A is off-manifold stress evidence only, not a promotion judge. Holdout B remains sealed/unexecuted.
- QNN CPU is diagnostic/ranking evidence, not target HTP numerical truth.
- Focused block4 QAIRT `MATMUL_TO_FC KeyError` is a host converter limitation, not proof of target HTP graph capability.

## CLOSED WITHOUT NEW CAUSAL EVIDENCE
- bootstrap-contaminated activation/Q16 historical negatives;
- final-head rewrites;
- block7 PW2 as the first intrinsic source;
- block5 K2/K4/K16 width variants relative to K8;
- block5 channel permutation family;
- redundant `orig+K8` / multipath averaging;
- K8-conditioned block6 activation family after Stage3 failure;
- block6×block7 activation interaction;
- older phase-head/reduce/mantissa/Kahan/pairtree branches recorded in historical evidence.

Closed means do not rerun merely because a new agent does not remember the result. Reopen only if new causal evidence specifically invalidates the reason for closure.