你是一个**纯 handoff 还原度测试 agent**，不是开发 agent。请假设你完全不知道此前聊天内容，也不要尝试继续 MeanVC2/REV46 开发。你的唯一任务是从 `/mnt/data/rev46_sandbox/handoff_v2/READ_FIRST.md` 开始，严格按照 handoff v2 指针进行只读恢复审计：读取它引用的 `CURRENT_STATE.json`、`EXACT_NEXT.json`、`EXPERIMENT_LEDGER.jsonl`、`ENVIRONMENT_AND_LAYOUT.md`、`RECOVERY_ANCHORS.md` 等文件，并运行 `python3 /mnt/data/rev46_sandbox/handoff_v2/validate_handoff.py`；**绝对不要运行** `resume_A_B_C_qnn_stage1.sh`，不要运行任何模型/QNN/ORT 推理，不要编译 QAIRT，不要构建 APK，不要跑设备，不要改任何实验/模型/handoff 文件。请仅根据 handoff 还原并核验：当前 host best 的路径、SHA 和指标；仍失败的冻结 gate；APK/device 是否允许；当前唯一 active experiment 及科学假设；A/B/C 候选及 SHA；哪些阶段已完成、哪些尚未开始；唯一 exact next；至少 5 项禁止动作/禁止重跑事项；ORT/QNN 关键布局合同；近期 INVALID/NON-AUTHORITATIVE 证据；完整沙盒丢失时的外部恢复锚点；以及 handoff 中是否存在冲突、过期指针、缺失文件或 SHA 漂移。按 GitHub 上 `evidence/rev46/handoff-v2/TEST_AGENT_PROTOCOL.md` 的 100 分规则评分。最后只创建一份新报告到 `lly8666/qairt-sdk-archive` 的 `evidence/rev46/handoff-tests/20260821-fresh-agent-handoff-v2-report.md`，报告必须包含：总分、逐项还原结果、validator 原始结论、发现的歧义/缺失、可能导致新 agent 误操作的风险、以及具体可执行的 handoff 改进建议。不要做除此之外的任何工作。